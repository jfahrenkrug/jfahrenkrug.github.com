---
import Icon from './Icon.astro';
---

<section class="box skills">
	<div class="stack gap-2 lg:gap-4">
		<Icon icon="strategy" color="var(--accent-regular)" size="2.5rem" gradient />
		<h2>Problem Solver</h2>
		<p>I untangle tricky bugs and performance bottlenecks across iOS, frontend, and backend systems.</p>
	</div>
	<div class="stack gap-2 lg:gap-4">
		<Icon icon="trophy" color="var(--accent-regular)" size="2.5rem" gradient />
		<h2>Skill Builder</h2>
		<p>I help teams get up to speed with iOS and practical AI. Always hands-on, never 'death by PowerPoint.'</p>
	</div>
	<div class="stack gap-2 lg:gap-4">
		<Icon icon="hammer" color="var(--accent-regular)" size="2.5rem" gradient />
		<h2>Craftsman</h2>
		<p>I care deeply about clean, maintainable code and the small details that make software a pleasure to work with.</p>
	</div>
</section>

<style>
.box {
  border: 1px solid var(--gray-800);
  border-radius: 0.75rem;
  padding: 1.75rem;               /* + */
  background-color: var(--gray-999_40);
  box-shadow: var(--shadow-sm);
}

.skills {
  display: flex;
  flex-direction: column;
  gap: 2.25rem;                    /* a bit tighter on mobile */
}

.skills h2 {
  font-size: var(--text-xl);       /* up from lg */
  line-height: 1.25;
}

.skills p {
  color: var(--gray-300);          /* slightly higher contrast */
  font-size: var(--text-base);     /* up from default */
  line-height: 1.6;                /* improve readability */
  max-width: 60ch;                 /* avoid long lines */
  margin: 0;                       /* rely on stack gaps */
}

@media (min-width: 50em) {
  .box {
    border-radius: 1.25rem;
    padding: 2.25rem;
  }

  .skills {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 3rem;                     /* a bit tighter than 5rem */
  }

  .skills h2 {
    font-size: var(--text-2xl);
  }

  .skills p {
    font-size: var(--text-lg);     /* comfortably large on desktop */
    line-height: 1.7;
  }
}
</style>
